{
  "name": "nodejs-pr-automation",
  "version": "1.0.0",
  "description": "Node.js PR Automation Project",
  "main": "index.js",
  "scripts": {
    "lint": "echo 'Running lint' && exit 0",
    "test": "echo 'Running tests' && exit 0",
    "build": "echo 'Running build' && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}